import RMobileHeader from './RMobileHeader.vue';

/* istanbul ignore next */
RMobileHeader.install = function install (Vue) {
  Vue.component('r-mobile-header', RMobileHeader);
};

export default RMobileHeader;
