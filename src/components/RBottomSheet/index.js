import RBottomSheet from './RBottomSheet.vue';

/* istanbul ignore next */
RBottomSheet.install = function install (Vue) {
  Vue.component('r-bottom-sheet', RBottomSheet);
};

export default RBottomSheet;
